import React from 'react';
import ContentContainer from '@containers/ContentContainer';

const HomePage = (): JSX.Element => (
  <ContentContainer>PRIVACY POLICY</ContentContainer>
);
export default HomePage;
